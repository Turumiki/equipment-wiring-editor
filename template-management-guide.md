# 📚 テンプレート管理システム完全ガイド

## 🎯 **概要**

テンプレート管理システムが大幅に強化されました！プロジェクト内テンプレートとグローバルテンプレートの両方を管理できるようになりました。

## 📊 **テンプレートの種類**

### **1. グローバルテンプレート**
- **保存場所**: アプリケーション設定
- **利用範囲**: 全てのプロジェクトで使用可能
- **用途**: よく使う標準的な機材テンプレート

### **2. プロジェクトテンプレート**
- **保存場所**: プロジェクトファイル内
- **利用範囲**: そのプロジェクトのみ
- **用途**: プロジェクト固有のカスタム機材

## 🔧 **主な機能**

### **テンプレート作成**
1. **機材を右クリック** → 「テンプレートとして保存」
2. **保存先を選択**:
   - 📁 **プロジェクト**: このプロジェクトのみで使用
   - 🌐 **グローバル**: 全プロジェクトで使用
3. **情報を入力**: 名前、カテゴリ、説明、タグ

### **テンプレート管理**
- **TemplateLibrary**で一元管理
- **グローバル/プロジェクト**の切り替え表示
- **検索・フィルタ**機能

### **インポート・エクスポート**

#### **一括操作**
- **📤 エクスポート**: 全テンプレートをJSONファイルで出力
- **📥 インポート**: JSONファイルからテンプレートを読み込み

#### **単体操作**
- **📤 単体エクスポート**: 個別テンプレートをJSONファイルで出力
- **📋 コピー**: グローバル ⇔ プロジェクト間でコピー

### **テンプレート操作**
- **🗑️ 削除**: 不要なテンプレートを削除
- **✏️ 編集**: テンプレート情報の更新（今後実装予定）

## 💾 **データ構造**

### **プロジェクトファイル**
```json
{
  "name": "スタジオA音響システム",
  "objects": [...],
  "wires": [...],
  "customTemplates": [
    {
      "id": "custom-mixer",
      "name": "カスタムミキサー",
      "category": "オーディオ",
      "ports": [
        { "side": "left", "offset": 25, "type": "xlr-female", "direction": "input", "label": "Ch 1" }
      ]
    }
  ]
}
```

### **テンプレートファイル（単体）**
```json
{
  "id": "yamaha-ql1-custom",
  "name": "YAMAHA QL1 カスタム",
  "category": "ミキサー",
  "description": "32ch デジタルミキサー",
  "ports": [...],
  "tags": ["YAMAHA", "ミキサー", "Dante"]
}
```

## 🚀 **使用方法**

### **1. テンプレート作成**
```typescript
// 機材を右クリック → 「テンプレートとして保存」
// または
const template = {
  id: 'my-custom-device',
  name: 'マイカスタム機材',
  ports: [
    { side: 'left', offset: 50, type: 'xlr-female', direction: 'input', label: 'Input' }
  ]
}
addTemplateToProject(template) // プロジェクトに追加
```

### **2. テンプレート使用**
```typescript
// TemplateLibraryから選択してクリック
// または
const equipment = createEquipmentFromTemplate(template)
addEquipmentObject(equipment)
```

### **3. テンプレート管理**
```typescript
// エクスポート
exportProjectTemplates()        // プロジェクトテンプレート全体
exportSingleTemplate(templateId) // 単体テンプレート

// インポート
importProjectTemplates(templates) // プロジェクトに追加
```

## 📋 **ワークフロー例**

### **プロジェクト固有機材の作成**
1. 基本機材を配置・カスタマイズ
2. 右クリック → 「テンプレートとして保存」
3. **プロジェクト**を選択して保存
4. 同じプロジェクト内で再利用

### **標準機材の共有**
1. よく使う機材をカスタマイズ
2. **グローバル**テンプレートとして保存
3. 他のプロジェクトでも使用可能

### **テンプレート配布**
1. カスタムテンプレートを作成
2. 単体エクスポートでJSONファイル生成
3. 他のユーザーがインポートして使用

## 🎨 **UI操作**

### **TemplateLibrary**
- **タブ切り替え**: グローバル ⇔ プロジェクト
- **ホバー操作**: マウスオーバーで管理ボタン表示
- **ワンクリック追加**: テンプレートクリックで機材追加

### **SaveTemplateDialog**
- **保存先選択**: ラジオボタンで直感的選択
- **カテゴリ管理**: 既存カテゴリ選択 or 新規作成
- **タグ付け**: カンマ区切りで複数タグ設定

## 🔄 **データフロー**

```
機材作成 → カスタマイズ → テンプレート保存
    ↓
[プロジェクト内] ⇔ [グローバル設定]
    ↓
エクスポート → JSONファイル → 他環境でインポート
```

## 🎯 **メリット**

1. **プロジェクト自己完結**: 全てのテンプレートがプロジェクトファイルに含まれる
2. **柔軟な共有**: 単体・一括両方のエクスポート/インポート対応
3. **効率的な作業**: よく使うテンプレートはグローバルで管理
4. **バージョン管理**: プロジェクトファイルと一緒にテンプレートも管理

これで、テンプレート管理が格段に使いやすくなりました！ 🎉